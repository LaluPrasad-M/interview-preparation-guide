# OOP

> [!tldr]
> Index for everything under `OOP/`. Object oriented design as a concept, with TypeScript as the example language because that is what the source notes used.

---

## Notes

| Note | Covers |
| --- | --- |
| [[abstract-classes]] | abstract classes and methods, with and without, abstract class against interface |
| [[access-modifiers]] | public, protected and private, and why TypeScript's private is not enforcement |

---

## Not written yet

The source notes covered abstraction and encapsulation only, so the rest of the subject has no notes: the four pillars as a set, inheritance and composition, polymorphism, SOLID, and design patterns.

Those arrive when there is real material for them rather than as empty headings.
